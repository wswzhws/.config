If:
  FileMatch: '.*\.c$'
  CompileFlags:
    Add: [-std=c23, ...]
If:
  FileMatch: '.*\.(cpp|cc|cxx)$'
  CompileFlags:
    Add: [-std=c++20, ...]
If:
  FileMatch: '.*\.h$'
  HeaderFilter: .*

InlayHints:
  Enabled: true
  DeducedTypes: true
  ParameterNames: true
